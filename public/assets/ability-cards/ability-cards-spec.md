# Ability Cards Tetris Flow Spec

## 0. Task Boundary

Implement only the lower half of the second screen: Ability Cards Tetris Flow module.

Do not modify:
- HeroSection
- Header / top navigation
- Logo
- dog element
- circle / ring animation
- Hero animation
- AbilityScenesIntroStage
- any SVG / coordinates / scale / reveal / parallax / buffer from the completed typography intro stage

Only append the new module after AbilityScenesIntroStage.

Do not commit changes unless explicitly asked.

After implementation, run:

```bash
npm run build